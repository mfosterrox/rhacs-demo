# Inside the VM console (you should still be connected):

# 1. Check the FULL cloud-init logs to see the actual error
sudo cat /var/log/cloud-init.log | grep -A 20 "runcmd"

# 2. Test network connectivity
ping -c 3 8.8.8.8
ping -c 3 mirror.openshift.com

# 3. Try to manually download roxagent to see the exact error
curl -v -k -L -o /tmp/test-roxagent https://mirror.openshift.com/pub/rhacs/assets/4.9.0/bin/linux/roxagent

# 4. Check if curl even exists
which curl
curl --version

# 5. Check DNS resolution
nslookup mirror.openshift.com
